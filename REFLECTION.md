# REFLECTION.md — 反思报告

## 1. 哪些 Superpowers 技能发挥了最大作用？
| 技能 | 作用 | 原因 |
|---|---|---|
| **brainstorming**|帮助user将粗糙的idea精细化为具体的设计方案|类似软件需求分析的步骤，user在提出的idea往往只是一个模糊的需求（比如：我想设计一个购物软件），很难提供专业且具体的软件需求规格说明书，将开发目标详细阐述出来。**brainstorming**技能通过不断追问用户问题，明确了用户开发的目的及期望达成的需求（如设计架构、开发代码量和成本的控制），这样才可以让agent开发出的产品更符合用户实际需求 |
| **writing-plans** | 基于**brainstorming**开发出的SPEC.md，设计agent开发的计划，包括划分tasks、安排tasks并行执行等等|PLAN让开发的过程更高效，project被划分成合适粒度的tasks，每个task交给一个subagent解决，tasks之间可并行解决。每个task都包括具体文件路径、失败测试、最小实现，强制执行测试驱动开发TDD |
| **test-driven-development** | 强制执行测试驱动开发TDD | 在TDD时沉浸于技术开发，而不是业务流程+业务边界+代码实现+设计，根据先前分好的task，把需求写成可验证的代码，通过测试强制agent满足需求 |
| **using-git-worktrees** | 在多agent执行同一个project的多任务的场景下，避免文件修改冲突 |模块隔离 + 合并卫生：每个subagent一个工作树，并行开发，互不干扰；同时由于每个subagent执行一个task，因此task模块高内聚，task模块之间低耦合 |
---
## 2. 哪些 Superpowers 技能"形式大于实质"？
| 技能 | 原因 |
|---|---|
| **executing-plans** | 可以将**executing-plans**的功能加入**subagent-driven-development**中，连续完成任务分配、TDD注入、subagent执行任务、执行与验收 |
| **dispatching-parallel-agents** | 在**using-git-worktrees**隔离出的工作区的基础上，安排subagent进入隔离区完成task。实际上无需将git worktrees这步分开执行，完全可以将dispatch的步骤纳入**using-git-worktrees**中 |
| **systematic-debugging** | 在开发过程中无实际用处，debugging部分由人工干预指出，未自动触发**systematic-debugging**，形式大于内容 |
---

## 3. TDD 在 AI 协作下：阻碍还是放大器？
一句话总结：TDD强制在AI协作下是**放大器**。

原因：尽管经过**brainstorming**技能之后，用户需求（无论是架构设计方案还是验收要求）已经较为明确了，但是自然语言表述依然抽象且不够形式化，导致agent编写出的代码只能满足部分需求，存在部分测试用例不通过的情况。TDD通过编写失败测试、最小实现、重构代码，明确代码的边界和形式化需求，确保用户需求高度覆盖，减少agent coding测试不通过概率。

---

## 4. subagent-driven 工作流让智能体能自主运行多久而不偏离主题？

**单任务、单文件范围、2–4 小时的 subagent，基本不偏离；一旦任务超过一个模块或依赖会话外上下文，就开始漂移。**

---

## 5. 什么样的 task 颗粒度最优？

**一个任务 = 一个可独立验收的模块/关切 + 一次完整红绿 + 一套几分钟内跑完的测试**。
- 太大（反例）：T1（harness骨架，独立但耗时长）、T24（整个 REPL）——返工最多、耗时最长；
- 合适：T22（hooks 扩展）、T21（上下文工程）、T28（测试基建）——单文件级改动，一次评审过；

---

## 6. SPEC / PLAN 质量如何影响实现质量？
SPEC / PLAN 的质量高度影响实现质量，这也是我花在项目上时间最多的部分，真正的subagent coding部分反而没操什么心。对于SPEC.md中没提到/模糊的需求，subagent不会实现/执行结果不尽如人意，需要后期返工修改多处。PLAN.md中task划分粒度足够细，可以提升开发效率（subagent可并行执行，无需串行等待）。

**案例1：安全机制中Sandbox的设计**

- SPEC.md中Sandbox一开始仅包含local进程策略设计，Docker隔离只作为桩模块等后续升级；实现后认为Sandbox隔离力度不够，加入Docker深度隔离执行环境。

**教训**：SPEC.md中需明确规定技术选型再确认实施，好在初期设计方案是local进程策略拦截+后期Docker桩模块可替换，因此无需太多改动即可加入Docker隔离。SPEC.md是实现的基础。

**案例2：PLAN.md中task划分粒度问题**

- Task1在所有任务中必须首先执行，且其他任务必须等task1执行完之后方可启动。但由于Task1是项目骨架，粒度划分过大导致实际运行时间长，其他任务都卡在开始无法并行执行。

**教训**：PLAN.md是提升实现效率的关键。

---

## 7. 最有效的 prompt / context 策略是什么、为什么有效？

**最有效的策略：结构化任务 brief + 具体要求。** 形如：

```
【任务】…（一个动词，一个对象）
【边界】不做 X、不改 Y、不加注释、不加库
【要求】先写失败测试（RED）→ 实现（GREEN）→ 全量回归
【验收】命令 + 预期输出
```

为什么有效：

1. **边界比任务更重要**：AI 的默认行为是"多做"（加参数、改格式、重构无关代码）。明确"不做什么"比"做什么"更能防漂移。
2. **上下文越少越好**：subagent 只拿到自己那段 PLAN + brief，不背全量对话；上下文长则注意力稀释。


---

## 8. 凭据与分发两条工程要求，迫使想清楚了哪些原本会忽略的问题？

**凭据（keyring / .env / 向导 / 验证）**：

1. **凭据如何安全录入 / 存储  / 修改 / 删除**：key不可硬编码进源码、绝不提交进 Git（含历史）、绝不写⼊⽇志 / 终端 history / 明⽂配置⽂件；要求开发者必须根据自身条件，选择一种安全存储的方式
2. **明文威胁模型**：考虑可能泄露key的各种威胁模型与对策，完善代码

**分发（PyPI 为主 + 源码为辅）**：

1. **代码结构干净**：同步commit到github仓库，代码结构必须干净可控，包含README.md，方便别人阅读&理解
2. **分发的方便性和开源性**：配置步骤/指令尽量简单、快捷，直接git clone之后pip install
3. **多平台适配**：个人的LLM API使用硅基流动配置，不同于DEEPSEEK官方API配置。一旦要给别人用，代码写死就不可接受。

---

## 9. 如果重做，我会改变什么？

1. **"真实边界验证清单"尽早验证**：Docker、真 LLM、真API key——从一开始就排期，而不是等实现之后再验证。
2. **brainstorming阶段就确定好需求/为后续升级留好空间，后期不再改动**：避免工作流返工修改
3. **Task细粒度划分**：避免大task占用时间。

---

## 10. 对 Superpowers 方法论的批判

**它假设了什么？--哪些假设不成立**

1. **假设有一个权威的人类 principal 在循环里**：两阶段评审、裁决、干预都依赖"人"。本项目的真实情况是：用户只在两个关键点介入：SPEC和PLAN方案设计、成果验收，其余时间**控制器既是评审者又是裁决者**——agent自我评审的独立性是欠缺的。
2. **假设写下来的计划/规格能消除歧义**：方法论把大量精力放在"写清楚"上，但自然语言歧义无法靠写来消除——**需要的是更早的独立验证**（评审、真实执行），而验证被放在了流程后段。
3. **假设新上下文 + 详细 brief 的成本可接受**：每个任务全量重新读模块、重跑上下文，token 和时间开销真实存在。
4. **假设技能内容稳定不重叠**：TDD 与 verification-before-completion、systematic-debugging 边界模糊，实际使用时大量重叠，没有带来对应收益。

**哪些成立？**

- TDD 先测试再开发的工作流成立；worktree 隔离任务防止subagent互相干扰成立；

---

