[project]
name = "coding-agent-harness"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "requests",
    "mcp",
    "keyring",
]

[tool.pytest.ini_options]
testpaths = ["harness/tests"]
addopts = "-q"